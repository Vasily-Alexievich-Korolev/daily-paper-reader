---
title: Variable-Rate Deep Image Compression based on Low-Rank Adaptation by Progressive Learning
title_zh: 基于渐进学习的低秩自适应变速率深度图像压缩
authors: "Xing-Yu Xu, Chen-Hsiu Huang, Ja-Ling Wu"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16107v1"
tags: ["query:board-search"]
score: 10.0
evidence: 使用低秩自适应进行变速率图像压缩
tldr: "变速率深度图像压缩面临多模型参数冗余或单模型性能低下的挑战。本文提出基于低秩自适应(LoRA)的逐步学习方法，在压缩模型中插入可合并的速率自适应模块(LoRAM)，推理时不增加额外计算。该方法在保持与多模型方法相当性能的同时，参数存储节省99%，数据集减少90%，训练步骤减少97%。首次将LoRA参数高效微调用于变速率图像压缩，大幅降低资源消耗。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16107v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 841, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16107v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 843, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16107v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16107v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 866, \"height\": 317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16107v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 868, \"height\": 318, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16107v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 636, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16107v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 780, \"height\": 160, \"label\": \"Table\"}]"
motivation: 现有变速率深度图像压缩方法存在多模型参数冗余或单模型性能低下的问题。
method: 提出基于低秩自适应(LoRA)的逐步学习框架，在压缩模型中插入可合并的速率自适应模块。
result: "与多模型方法性能相当，参数存储节省99%，数据集减少90%，训练步骤减少97%。"
conclusion: 首次将LoRA参数高效微调应用于变速率图像压缩，大幅降低存储和训练成本。
---

## 摘要
在数字时代，图像压缩对于众多应用至关重要，包括网络媒体、流媒体服务、高分辨率医学成像和车联网，能够实现高效的数据存储和传输。随着对高质量图像通信需求的增长，对先进压缩技术的需求变得越来越关键。最近，许多深度图像压缩（DIC）技术被提出，与传统标准相比显示出令人印象深刻的性能。然而，变速率图像压缩仍然是一个未解决的问题。特定的DIC方法部署多个网络以获得不同的压缩率，而其他方法则使用单个模型，这通常会导致更高的计算复杂度和性能下降。本文提出了一种基于参数高效微调方法——低秩自适应（LoRA）的渐进学习变速率图像压缩方法。我们在DIC方法中引入了一个额外的LoRA速率自适应模块（LoRAM）。由于LoRA的重参数化合并，我们提出的方法在推理过程中不引入额外的计算复杂度。与使用多个模型的方法相比，综合实验表明，我们的方法实现了有竞争力的性能，节省了99%的参数存储、90%的数据集和97%的训练步骤。

## Abstract
In the digital age, image compression is crucial for numerous applications, including web media, streaming services, high-resolution medical imaging, and connected vehicle networks, enabling efficient data storage and transmission. With the increasing demand for high-quality image communication, the need for advanced compression techniques becomes increasingly critical. Numerous Deep Image Compression (DIC) techniques have recently been introduced, showing impressive performance compared to traditional standards. However, variable-rate image compression remains an unresolved issue. Specific DIC methods deploy multiple networks to attain different compression rates, whereas others use a single model, which often results in higher computational complexity and reduced performance. This work proposes a progressive learning approach for variable-rate image compression based on the parameter-efficient fine-tuning method, the Low-Rank Adaptation (LoRA). We introduce an additional LoRA Rate-Adaptive Module (LoRAM) in DIC methods. Due to the re-parameterized merging of LoRA, our proposed method does not introduce additional computational complexity during inference. Compared to methods utilizing multiple models, comprehensive experiments demonstrate that our approach achieves competitive performance, saving 99\% in parameter storage, 90% in datasets, and 97% in training steps.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：深度图像压缩（DIC）在数字时代至关重要，但**变速率压缩**仍是尚未解决的难题。现有方法要么训练多个独立模型对应不同码率（导致参数冗余、训练和存储成本高），要么使用单一模型但性能下降或计算复杂度增加。
- **整体含义**：作者旨在提出一种**参数高效、训练快速、推理无额外开销**的变速率深度图像压缩方法，首次将自然语言处理中的**低秩自适应（LoRA）** 微调范式引入图像压缩领域，大幅降低资源消耗的同时保持与多模型方案相当的率失真性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：基于**渐进学习**（Progressive Learning）策略，先预训练一个骨干模型（对应一个固定λ₀），然后针对不同目标码率（不同λᵢ）分别训练**LoRA速率自适应模块（LoRAM）**，仅微调极少量参数。推理时，LoRAM可合并回原始权重中，不增加推理延迟。
- **关键技术细节**：
  - **骨干网络**：采用两种现有架构——**Window-Attention CNN (WACNN)** 和 **Symmetrical TransFormer (STF)**。编码器、解码器、超编码器、超解码器及熵参数预测器均使用标准VAE结构。
  - **LoRAM 设计**：对于每个线性层（在卷积层中也通过等效线性变换实现），保持原权重矩阵 \(W_k\) 冻结，添加低秩分解 \(\Delta W_k = B_k A_k\)，其中 \(A_k \in \mathbb{R}^{r \times c_{\text{in}}}\)（高斯初始化），\(B_k \in \mathbb{R}^{c_{\text{out}} \times r}\)（零初始化）。秩 \(r\) 远小于输入输出维度。初始化时 \(\Delta W_k = 0\)。
  - **层选择**：只在骨干网络的**浅层**（负责捕获低级图像细节的区域）插入LoRAM，具体为WACNN中红色虚线框内的所有层（包括窗口注意力模块中的Q/K/V/g变换及残差后的线性层），以及STF中每个Swin Transformer Block的Q/K/V、MLP全连接层。这样做以最少额外参数实现相近性能。
  - **训练过程**：
    1. **第一阶段**：固定 \(λ₀ = 0.025\)，仅训练骨干网络参数（编码器、解码器、超先验等），使用300k张256×256图像，训练1.8M步，学习率1e-4，batch size 16，Adam优化器。
    2. **第二阶段（LoRA微调）**：针对不同λᵢ ∈ {0.0018, 0.0035, 0.0067, 0.013, 0.0483}，分别训练对应的LoRAM（\(\Delta\phi_i, \Delta\omega_i, \Delta\theta_i, \Delta\psi_i, \Delta\xi_i\)）。仅使用32k张图像，训练40k步，学习率5e-4，batch size 16，Adam优化器。
  - **损失函数**：标准率失真损失 \(L = R + \lambda_i \cdot D\)，其中R为码率（基于高斯熵模型的负对数似然），D为MSE失真（PSNR评估时转换）。

## 3. 实验设计

- **数据集与评估场景**：
  - **训练集**：第一阶段使用OpenImages数据集（随机300k张），第二阶段使用其子集（32k张），均裁剪为256×256。
  - **测试集**：
    - **率失真性能**：Kodak图像集（24张标准测试图）和CLIC专业验证集（2020版）。
    - **下游分类任务影响**：从ImageNet1k每类随机选取10张，共1万张图像，用预训练ResNet18评估Top-1和Top-5准确率。
- **基准（Benchmark）**：
  - 对比方法：WACNN和STF（原始单模型）、Cheng et al. (2020) 的离散高斯混合似然模型、经典编码器JPEG和VTM（VVC参考软件）。
  - 对比维度：率失真曲线（bpp vs PSNR）、分类准确率、参数存储量、数据集大小、训练步数。

## 4. 资源与算力

- **文中未明确说明GPU型号、数量、卡时等具体硬件信息**。仅提及使用Adam优化器，batch size和训练步数。实际算力需求可推测：骨干网络训练1.8M步（300k图像、256×256），LoRAM微调仅40k步（32k图像），整体算力需求远低于从头训练多个独立模型。但论文未给出精确的GPU规格。

## 5. 实验数量与充分性

- **实验数量**：
  - 率失真性能：在Kodak和CLIC两个数据集上，分别对WACNN和STF两种骨干网络进行了测试，并对比了4种baseline。
  - 分类任务影响：在ImageNet子集上比较了不同码率下的Top-1/Top-5准确率，并与全训练模型、原始图像基线对比。
  - 效率对比：表格展示了参数存储节省99%、数据集减少90%、训练步骤减少97%。
- **充分性与公平性**：
  - **优点**：实验覆盖了标准测试集和下游任务验证，对比了当前主流方法和传统编码器，结果展示清晰（含曲线图）。
  - **不足**：
    - 缺乏**消融实验**（如不同秩r的影响、不同层选择的影响、不同λ序列的影响）。
    - 仅使用MSE失真（PSNR），未使用SSIM或MS-SSIM等感知指标。
    - 仅在256×256裁剪图像上训练和测试，对更大尺寸图像（如CLIC中分辨率更高的图像）可能出现性能退化（作者已指出WACNN+LoRA在CLIC上略有下降）。
    - 分类任务测试集较小（每类10张，共1万张），可能不够鲁棒。
    - 未与最新的变速率方法（如条件自编码器、空间特征变换等）进行直接对比，仅比了固定码率模型和传统编码器。

## 6. 主要结论与发现

- **性能可比**：提出的LoRAM方法在率失真性能上与独立训练的WACNN/STF模型非常接近，在Kodak和CLIC上均达到可比水平，且优于JPEG，接近VTM。
- **高效性**：仅需0.2%的额外参数（相对于骨干网络），即可实现多个不同码率的压缩模型；推理时无额外延迟（可合并）
- **训练效率**：使用10.6%的原始数据集（32k vs 300k）和2.2%的训练步数（40k vs 1.8M），即可收敛。参数存储节省99%。
- **下游任务无负影响**：在ImageNet分类任务上，LoRAM微调模型与全训练模型的准确率几乎一致，表明LoRA未引入有害偏差。

## 7. 优点

- **参数极高效**：每个码率仅需0.2%额外参数，存储成本极低。
- **推理零开销**：LoRA权重可合并到原权重中，不增加推理延迟。
- **训练样本与时间大幅节省**：仅用少量数据（32k张）和极少训练步数（40k步）即达到全训练性能。
- **易于集成**：可即插即用于任何基于线性层/卷积的DIC架构（WACNN、STF）。
- **首次将LoRA引入变速率图像压缩**，开辟了参数高效微调在该领域的新思路。

## 8. 不足与局限

- **技术局限**：
  - 每个目标码率仍需单独训练一组LoRAM（虽然参数极少），不能像条件模型那样连续调节。
  - 层选择仅为经验性，缺乏理论指导或自动搜索。
  - 秩r的设定固定（具体数值未明确给出，仅说r ≪ min{cin, cout}），未探索最优r。
- **实验覆盖不足**：
  - 仅评估PSNR，未用感知指标（SSIM等）。
  - 未与多种变速率方法（如条件自编码器、空间特征变换、混合bin大小等）进行公平比较。
  - 无消融研究（如不同秩、不同深度的LoRAM效果、不同λ序列影响）。
  - 分类任务测试集规模小、仅用ResNet18，泛化性受限。
- **应用限制**：
  - 训练时需固定图像尺寸（256×256），对大尺寸或非正方形图像性能可能下降（已在CLIC上观察到WACNN变体性能下降）。
  - 依赖预训练骨干网络，若骨干网络本身不是最优，LoRAM无法根本改善。
- **偏差风险**：仅使用OpenImages数据集训练，可能对其他域（如医学、遥感）图像泛化不佳。

（完）
